<!doctype html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Kayıt Ol • Kelime App</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/style.css" />
</head>

<body class="min-h-screen bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-600 flex items-center justify-center p-6">
  <div class="w-full max-w-md">
    <div class="glass rounded-3xl p-6 md:p-10 shadow-2xl">
      <h1 class="text-3xl font-extrabold text-white tracking-tight">Kayıt Ol</h1>
      <p class="text-white/80 mt-1">Kelime App hesabını oluştur</p>

      <% if (error) { %>
        <div class="mt-5 rounded-2xl bg-red-500/20 border border-red-200/30 p-4 text-white">
          <span class="font-semibold">Hata:</span> <%= error %>
        </div>
      <% } %>

      <form action="/register" method="POST" class="mt-6 space-y-3">
        <input
          name="username"
          value="<%= form.username %>"
          placeholder="Kullanıcı adı"
          class="w-full rounded-2xl px-4 py-3 outline-none bg-white/90 focus:bg-white transition shadow"
        />
        <input
          name="email"
          value="<%= form.email %>"
          placeholder="Email"
          type="email"
          class="w-full rounded-2xl px-4 py-3 outline-none bg-white/90 focus:bg-white transition shadow"
        />
        <input
          name="password"
          placeholder="Şifre"
          type="password"
          class="w-full rounded-2xl px-4 py-3 outline-none bg-white/90 focus:bg-white transition shadow"
        />

        <button class="w-full rounded-2xl px-5 py-3 font-semibold text-white bg-black/40 hover:bg-black/55 transition shadow">
          Kayıt Ol
        </button>
      </form>

      <div class="mt-6 text-center text-white/70 text-sm">
        Zaten hesabın var mı?
        <a href="/login" class="text-white font-semibold hover:underline">Giriş yap</a>
      </div>
    </div>
  </div>
</body>
</html>
