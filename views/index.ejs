<!doctype html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Kelime App</title>
  
  <!-- Tailwind CDN (kolay ve güzel tasarım için) -->
  <script src="https://cdn.tailwindcss.com"></script>

  <link rel="stylesheet" href="/style.css" />
</head>
<body class="min-h-screen bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-600 flex flex-col items-center p-6 pt-24">


    <% if (user) { %>
  <header class="fixed top-0 left-0 w-full z-50">
    <div class="glass border-b border-white/15">
      <div class="max-w-5xl mx-auto px-6 py-3 flex items-center justify-between">
        <div class="text-white">
          <div class="text-white/70 text-[11px] leading-none">Hesap</div>
          <div class="font-extrabold text-base md:text-lg">
            Merhaba, <span class="text-white"><%= user.username %></span> 
          </div>
        </div>

        <a href="/logout"
           class="rounded-2xl px-4 py-2 font-semibold text-white bg-black/40 hover:bg-black/55 transition shadow">
          Çıkış
        </a>
      </div>
    </div>
  </header>
<% } %>


  <div class="w-full max-w-2xl mx-auto flex-1 flex items-center justify-center">

    <div class="glass rounded-3xl p-6 md:p-10 shadow-2xl">
      <div class="flex items-center justify-between gap-4">
        <div>
          <h1 class="text-3xl md:text-4xl font-extrabold text-white tracking-tight">Kelime App</h1>
          <p class="text-white/80 mt-1">TR ↔ EN çeviri + random örnek cümle</p>
        </div>
        <div class="hidden md:block text-white/80 text-sm">
          Node.js • MySQL
        </div>
      </div>

      <form action="/search" method="POST" class="mt-6">
        <label class="block text-white/90 mb-2 font-medium">Kelime gir</label>
        <div class="flex gap-3">
          <input
            name="word"
            value="<%= query %>"
            placeholder="Örn: elma / apple"
            class="w-full rounded-2xl px-4 py-3 outline-none bg-white/90 focus:bg-white transition shadow"
          />
          <button class="rounded-2xl px-5 py-3 font-semibold text-white bg-black/40 hover:bg-black/55 transition shadow">
            Bul
          </button>
        </div>
      </form>

      <% if (error) { %>
        <div class="mt-5 rounded-2xl bg-red-500/20 border border-red-200/30 p-4 text-white">
          <span class="font-semibold">Hata:</span> <%= error %>
        </div>
      <% } %>

      <% if (result) { %>
        <div class="mt-6 grid md:grid-cols-2 gap-4">
          <div class="rounded-2xl bg-white/10 border border-white/20 p-5">
            <p class="text-white/80 text-sm">Girdiğin kelime</p>
            <p class="text-white text-2xl font-bold mt-1"><%= result.input %></p>
            <p class="text-white/70 text-sm mt-2">
              Dil: <span class="font-semibold"><%= result.inputLang.toUpperCase() %></span>
            </p>
          </div>

          <div class="rounded-2xl bg-white/10 border border-white/20 p-5">
            <p class="text-white/80 text-sm">Çeviri</p>
            <p class="text-white text-2xl font-bold mt-1"><%= result.translation %></p>
            <div class="mt-3 text-white/75 text-sm">
              <div>TR: <span class="font-semibold"><%= result.tr %></span></div>
              <div>EN: <span class="font-semibold"><%= result.en %></span></div>
            </div>
          </div>

          <div class="md:col-span-2 rounded-2xl bg-white/10 border border-white/20 p-5">
            <p class="text-white/80 text-sm">Random örnek cümle</p>
            <p class="text-white text-lg font-semibold mt-2 leading-relaxed">“<%= result.example %>”</p>
          </div>
        </div>
      <% } %>

      <div class="mt-8 text-center text-white/60 text-sm">
        İpucu: Veritabanına yeni kelime ve cümle ekleyerek büyütebilirsin.
      </div>
    </div>
  </div>

</body>
</html>
