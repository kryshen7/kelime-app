<!doctype html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Kelime App</title>
  <link rel="stylesheet" href="/style.css" />
</head>

<body>
  <% if (user) { %>
    <div class="topbar">
      <div class="wrap glass">
        <div>
          <div class="mini">Hesap</div>
          <div class="user">Merhaba, <%= user.username %> </div>
        </div>
        <a href="/logout">Çıkış</a>
      </div>
    </div>
  <% } %>

  <div class="page">
    <div class="container glass">
      <div class="header">
        <div>
          <h1 class="h-title">Kelime App</h1>
          <p class="h-sub">TR ↔ EN çeviri + random örnek cümle</p>
        </div>
        <div class="badge">Node.js • MySQL</div>
      </div>

      <form action="/search" method="POST" class="form">
        <label class="label">Kelime gir</label>
        <div class="row">
          <input
            class="input"
            type="text"
            name="word"
            placeholder="Örn: elma / apple"
            value="<%= query || '' %>"
          />
          <button class="btn" type="submit">Bul</button>
        </div>
      </form>

      <% if (error) { %>
        <div class="alert"><b>Hata:</b> <%= error %></div>
      <% } %>

      <% if (result) { %>
        <div class="grid">
          <div class="card">
            <div class="muted">Girdiğin kelime</div>
            <div class="big"><%= result.input %></div>
            <div class="muted">Dil: <b><%= (result.inputLang || "").toUpperCase() %></b></div>
          </div>

          <div class="card">
            <div class="muted">Çeviri</div>
            <div class="big"><%= result.translation %></div>
            <div class="muted">TR: <b><%= result.tr %></b></div>
            <div class="muted">EN: <b><%= result.en %></b></div>
          </div>

          <div class="card full">
            <div class="muted">Random örnek cümle</div>
            <div class="example">“<%= result.example %>”</div>
          </div>
        </div>
      <% } %>

      <div class="tip">
        İpucu: Admin panelden yeni kelime ve cümle ekleyerek büyütebilirsin.
      </div>
    </div>
  </div>
</body>
</html>
