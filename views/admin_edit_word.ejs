<!doctype html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Kelime Düzenle • Kelime App</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/style.css" />
</head>

<body class="min-h-screen bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-600 flex flex-col items-center p-6 pt-24">

  <header class="fixed top-0 left-0 w-full z-50">
    <div class="glass border-b border-white/15">
      <div class="max-w-5xl mx-auto px-6 py-3 flex items-center justify-between">
        <div class="text-white font-extrabold">Kelime App • Düzenle</div>
        <a href="/admin/words" class="text-white/90 hover:underline">← Geri</a>
      </div>
    </div>
  </header>

  <div class="w-full max-w-xl mx-auto flex-1 flex items-center justify-center">
    <div class="glass rounded-3xl p-6 md:p-10 shadow-2xl w-full">
      <h1 class="text-2xl font-extrabold text-white">Kelime Düzenle</h1>

      <% if (error) { %>
        <div class="mt-5 rounded-2xl bg-red-500/20 border border-red-200/30 p-4 text-white">
          <span class="font-semibold">Hata:</span> <%= error %>
        </div>
      <% } %>

      <form action="/admin/words/<%= word.id %>/edit" method="POST" class="mt-6 space-y-3">
        <input name="tr" value="<%= word.tr %>" class="w-full rounded-2xl px-4 py-3 outline-none bg-white/90 focus:bg-white transition shadow"/>
        <input name="en" value="<%= word.en %>" class="w-full rounded-2xl px-4 py-3 outline-none bg-white/90 focus:bg-white transition shadow"/>
        <button class="w-full rounded-2xl px-5 py-3 font-semibold text-white bg-black/40 hover:bg-black/55 transition shadow">
          Kaydet
        </button>
      </form>
    </div>
  </div>
</body>
</html>
